This is a test project to illustrate the Hyperbolic geometry, one of the two types of Non-Euclidean geometries.

The code uses the library freeglut, so run
  apt-get install freeglut
before compiling.

This branch is an simple snake game. Use wasd to move around.

Now being written by owen and shsty.
